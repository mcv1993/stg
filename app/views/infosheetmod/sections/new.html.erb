<br />

  <h1>Add a new section</h1>

<%= simple_form_for @section, url: infosheetmod_client_sections_path(@client) do |f| %>
          <%= f.error_notification %>

          <div class="form-inputs">
            <%= f.input :title,
                        required: true,
                        autofocus: true%>
          </div>

          <div class="form-actions">
            <%= f.submit "Create a New Section" %>
          </div>
        <% end %>