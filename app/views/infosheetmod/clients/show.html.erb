<%= current_client.clientname %> Show Page
<br />
-list sections
<br />



<script>
$(function show_link_list() {
 

  $.get("/infosheets/clients/:client_id/sections").success( function( data ) {
    $.each(data, function(index, section) {
                // Create anchor element. 
                var a = document.createElement('a');  
                var ulist = document.getElementById("list1");
                var newItem = document.createElement("li");

                a.textContent = section.title;
                a.setAttribute('href', '/infosheets/clients/' + section.client_id + '/sections/' + section.id );
                newItem.appendChild(a);
                ulist.appendChild(newItem); 
    });
  });

});
</script>